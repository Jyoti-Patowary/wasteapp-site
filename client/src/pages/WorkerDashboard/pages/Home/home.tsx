import React from "react";
import { Button } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Button sx={{ p: "20rem", bg: "red" }}>Accept</Button>
    </div>
  );
};

export default Home;
